baseURL: "https://antozmtaani-hue.github.io/free/"
languageCode: "en-us"
title: "SkyLight Wings — Ultralight Aircraft Blog"
theme: "blowfish"
enableRobotsTXT: true

params:
  description: "Your comprehensive guide to ultralight aircraft, sport flying, and the freedom of personal aviation."
  author: "SkyLight Wings"
  propeller:
    site_id: "{{ PROP_ID }}"
    above_fold_slot: "above-fold-{{ PROP_ID }}"
    in_content_slot: "in-content-{{ PROP_ID }}"
    sidebar_slot: "sidebar-{{ PROP_ID }}"
  lighthouse_target: 95

markup:
  goldmark:
    renderer:
      unsafe: true

outputs:
  home:
    - HTML

menu:
  main: []
